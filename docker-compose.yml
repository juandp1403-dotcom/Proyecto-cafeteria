services:
  web:
    build: .
    ports:
      - "5001:5000"
    volumes:
      - .:/app
    env_file:
      - path: .env
        required: false
    environment:
      - FLASK_ENV=development
      - DATABASE_URL=sqlite:///cafeteria.db
