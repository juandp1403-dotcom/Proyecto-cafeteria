name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - run: pip install ruff
      - run: ruff check .

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: pip install pytest pytest-cov
      # HU-24: cobertura minima del nucleo de negocio (ventas/stock) --
      # falla el build si baja del umbral acordado en la auditoria.
      - run: >
          pytest tests/ -v
          --cov=blueprints.cliente.routes
          --cov=blueprints.admin.routes
          --cov=blueprints.admin.ventas
          --cov=blueprints.admin.productos
          --cov-report=term-missing
          --cov-fail-under=70

  pip-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - run: pip install pip-audit
      - run: pip install -r requirements.txt
      - run: pip-audit -r requirements.txt
